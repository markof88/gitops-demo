apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 7
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: app
        image: nginx:1.25
        ports:
        - containerPort: 80
        env:
        - name: SECRET_MESSAGE
          valueFrom:
            secretKeyRef:
              name: demo-secret
              key: message
        - name: TEAM_MESSAGE
          valueFrom:
            secretKeyRef:
              name: demo-secret
              key: team-message