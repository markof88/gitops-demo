resources:
  - ../../base
  # sealed-secret.yaml: add after sealing against the stage cluster key
  # kubeseal --controller-namespace kube-system --fetch-cert > stage-pub.pem
  # kubeseal --cert stage-pub.pem --format yaml < secret.yaml > sealed-secret.yaml

patches:
  - path: replica-patch.yaml
